# Angular on GitHub CodeSpaces

## Installation

1. Install node lts version(if not installed) using: ```nvm install --lts``` --> ```node --version``` --> ```nvm use --lts```

2. Install Angular using: ```npm install -g @angular/cli``` --> ```ng version```